# 导出决定
状态：用户已确认，2026-09-08。
本期为个人账单页面增加当前页 CSV 下载。列为 date、provider、amount_usd，顺序与页面一致；导出点击时已加载的页面数据，不重新请求。
空页输出表头。CSV 对逗号、双引号和换行按标准转义；已有错误提示表示页面加载失败时不生成文件。
用户已经确认仅导出当前页。全量导出、后台作业、下载历史本期不做。
按钮文案、默认文件名、内部函数拆分交由实现者选择。
