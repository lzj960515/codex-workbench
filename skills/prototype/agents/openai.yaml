interface:
  display_name: "Prototype"
  short_description: "Prototype to answer a design question"
  default_prompt: "Use $prototype to build a small logic demo or UI comparison that answers the current design question."

policy:
  allow_implicit_invocation: true
