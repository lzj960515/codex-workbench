# 导出决定
状态：用户已确认
管理员需要导出当前页面可见的数据，便于线下核对。仅导出当前页，保留该页筛选和排序结果；列固定为 name、status；UTF-8 CSV。空页仍输出列标题。列表读取失败时显示现有读取错误，不生成下载文件。不做全量导出、历史记录或后台作业。复用现有 visible_rows 取得当前页，CSV生成是本次新增能力。使用公开导出结果验证列、顺序、空页和失败行为，沿用 unittest。
