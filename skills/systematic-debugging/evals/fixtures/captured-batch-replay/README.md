# 批次接收归档
accept(events) 汇总一批上传条目，输入为归档事件列表，返回本批首次接收的条目ID。每个事件包含batch_id、item_id、trace_id、label。同一批里的不同item应全部接收；同一条目在同批重传应只接收一次。批次之间相互独立。
线上本批capture里有3个独立条目和2次重传，但接收结果只有1条。events.json是完整脱敏归档；expected.json是业务方根据提交清单确认的接收结果。没有线上服务、测试框架或现成重放命令。当前只要求在本地查明机制，先不修源码。
