interface:
  display_name: "Writing for Agents"
  short_description: "Write documents agents consume"
  default_prompt: "Use $writing-for-agents to write or improve this agent-facing document."

policy:
  allow_implicit_invocation: true
