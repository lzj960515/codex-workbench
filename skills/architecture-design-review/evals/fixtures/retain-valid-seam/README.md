# 账单汇总模块
BillingReport 汇总账单金额，Ledger.load(period) 提供账单条目。运行入口用 FileLedger 读本地CSV，测试用 MemoryLedger；二者表达同一账单读取契约。只有一个生产数据源，未来没有新增数据源的已确认计划。
账单读取失败由入口处理，汇总层传播异常；load返回的amount_cents由账单解析器保证为整数。本模块没有缓存、后台刷新、重试或持久化状态。
需要判断是否应为“只有一个生产实现”去掉当前接口，或保留现在的依赖边界。这轮只作架构判断。
