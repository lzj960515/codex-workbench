# 当前账户与API v2的供应商确认
每个账户按任意滚动1秒窗口允许30次请求；当前项目独占该账户，无其他请求流量。超额返回429及Retry-After。
POST导入在同一账户下，以Idempotency-Key及同一请求内容复用原始结果；并发相同请求也只创建一次。此保证从首次请求起持续24小时，覆盖响应丢失或客户端超时的情况；不同请求内容重用同一键返回冲突，不创建新导入。
上述结论由供应商技术负责人提供并已确认，当前接入环境中有效。
